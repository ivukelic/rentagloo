import React from 'react';
import styles from './Styleguide.module.css';

const Title = () => {
    return (
        <div className={styles.container}>Put components in here. fala.</div>
    );
};

export default Title;
